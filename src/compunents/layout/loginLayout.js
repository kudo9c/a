import Topbar from "../topbar/Topbar"


const loginLayout = ({children}) => {
    return (
        <>
            <main>
            {children}   
            </main>
        </>
    )
}

export default loginLayout
